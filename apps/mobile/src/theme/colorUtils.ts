/**
 * Utilitaires couleur — REMONTÉS dans `@tentacle-tv/theme`
 * (`src/utils/color.ts`) pour que le web construise son schéma clair avec les
 * mêmes helpers. Ce fichier n'est qu'un ré-export de compatibilité.
 */

export { darken, hexToRgb, withAlpha, type Rgb } from "@tentacle-tv/theme";
