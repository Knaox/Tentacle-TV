/**
 * Palette claire — REMONTÉE dans `@tentacle-tv/theme` (`src/schemes/light.ts`),
 * désormais partagée avec le web et le desktop. Ce fichier n'est qu'un
 * ré-export de compatibilité : les valeurs et le comportement sont inchangés.
 *
 * Les accents restent dérivés de `BRAND` vivant (post override admin) — voir
 * l'INVARIANT documenté à la source.
 */

export { buildLightPalette } from "@tentacle-tv/theme";
