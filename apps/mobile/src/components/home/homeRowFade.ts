/** Cascade d'entrée de l'accueil : la i-ème rangée entre un peu après la
 *  précédente — la position dans la mise en page, pas la nature de la rangée. */
export const homeRowFadeDelay = (index: number): number => 100 + index * 70;
