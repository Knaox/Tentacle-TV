import { OfflineLibraryScreen } from "@/offline/library/OfflineLibraryScreen";

/** Le catalogue hors ligne ouvert comme une page, depuis « Sur cet appareil » — même en ligne. */
export default function OfflineLibraryRoute() {
  return <OfflineLibraryScreen standalone />;
}
