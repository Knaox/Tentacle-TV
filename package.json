{
  "name": "tentacle-tv",
  "private": true,
  "version": "1.0.0-beta",
  "description": "Premium multi-platform media client",
  "scripts": {
    "dev:web": "pnpm --filter @tentacle-tv/web dev",
    "dev:backend": "pnpm --filter @tentacle-tv/backend dev",
    "dev:desktop": "pnpm --filter @tentacle-tv/desktop dev",
    "dev:mobile": "pnpm --filter @tentacle-tv/mobile start",
    "dev:tv": "pnpm --filter @tentacle-tv/tv start",
    "build:web": "pnpm --filter @tentacle-tv/web build",
    "build:backend": "pnpm --filter @tentacle-tv/backend build",
    "build:desktop": "pnpm --filter @tentacle-tv/desktop build",
    "build:tv:android": "pnpm --filter @tentacle-tv/tv android",
    "build:mobile:android": "pnpm --filter @tentacle-tv/mobile android",
    "build:mobile:ios": "pnpm --filter @tentacle-tv/mobile ios",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r exec rm -rf node_modules dist .turbo && rm -rf node_modules",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:rebuild": "docker compose up -d --build",
    "docker:reset": "docker compose down -v && docker compose up -d --build"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@10.30.2",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "bcrypt",
      "esbuild",
      "prisma"
    ],
    "overrides": {
      "@react-native/community-cli-plugin>@react-native-community/cli-server-api": "14.1.2"
    }
  }
}
