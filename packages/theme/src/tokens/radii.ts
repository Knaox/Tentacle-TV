import type { RadiusTokens } from "../types";

/** Verbatim from `apps/web/src/theme/tokens.css` (--radius-* group). */
export const DEFAULT_RADIUS_TOKENS: RadiusTokens = {
  xs: "4px",
  sm: "6px",
  md: "8px",
  lg: "12px",
  xl: "16px",
  pill: "9999px",
};
