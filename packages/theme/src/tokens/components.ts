import type { ComponentSizeTokens } from "../types";

/** Verbatim from `apps/web/src/theme/tokens.css` (--logo-size-* group). */
export const DEFAULT_COMPONENT_TOKENS: ComponentSizeTokens = {
  logoSm: "28px",
  logoMd: "36px",
  logoLg: "56px",
  logoXl: "96px",
};
