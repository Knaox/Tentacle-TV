import type { BlurTokens } from "../types";

/** Verbatim from `apps/web/src/theme/tokens.css` (--blur-* group). */
export const DEFAULT_BLUR_TOKENS: BlurTokens = {
  overlay: "24px",
  modal: "20px",
  dropdown: "12px",
  sheet: "16px",
};
