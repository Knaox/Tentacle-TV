import type { LayoutTokens } from "../types";

/** Verbatim from `apps/web/src/theme/tokens.css` (layout group). */
export const DEFAULT_LAYOUT_TOKENS: LayoutTokens = {
  topnavHeight: "68px",
  topnavHeightMobile: "56px",
  tabbarHeight: "64px",
  rowGutterMobile: "16px",
  rowGutterDesktop: "56px",
};
